{
  "name": "directory-shaker",
  "version": "0.2.0",
  "description": "Convert JS files structure to object, extract data",
  "main": "dist/directory-shaker.js",
  "scripts": {
    "test": "jest",
    "test:w": "jest --watch",
    "build": "babel src -d dist",
    "prepublish": "rm -rf dist && npm run build",
    "precommit": "rm -rf dist && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/darianstlex/directory-shaker.git"
  },
  "keywords": [
    "directory",
    "object",
    "transform"
  ],
  "author": "Ivan Uzun",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/darianstlex/directory-shaker/issues"
  },
  "homepage": "https://github.com/darianstlex/directory-shaker#readme",
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.5",
    "@babel/preset-env": "^7.1.5",
    "babel-core": "7.0.0-bridge.0",
    "eslint": "^5.9.0",
    "husky": "^1.1.3",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "esm": "^3.0.84"
  }
}
